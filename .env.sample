# .env.sample - Образец файла переменных окружения.
# Скопируйте этот файл в .env.local для локальной разработки и заполните значения.
# Никогда не добавляйте .env.local в систему контроля версий.

# --- ПУБЛИЧНЫЕ ПЕРЕМЕННЫЕ (доступны на клиенте) ---

# Полный публичный URL вашего приложения. Используется для SEO и мета-тегов.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# URL для iframe с демонстрацией Streamlit (scenario-core).
NEXT_PUBLIC_STREAMLIT_URL=https://your-streamlit-demo-url.streamlit.app

# Публичный ключ Google reCAPTCHA v3.
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=<your_recaptcha_v3_site_key>

# DSN для интеграции с Sentry (отслеживание ошибок на клиенте и сервере).
NEXT_PUBLIC_SENTRY_DSN=<your_sentry_dsn>


# --- СЕРВЕРНЫЕ ПЕРЕМЕННЫЕ (доступны только на сервере) ---

# Секретный ключ Google reCAPTCHA v3 для серверной валидации.
# ВАЖНО: В production-режиме API не будет работать без этого ключа.
RECAPTCHA_SECRET=<your_recaptcha_v3_secret_key>

# Порог оценки reCAPTCHA для прохождения проверки (от 0.0 до 1.0).
# Значение по умолчанию в коде - 0.5.
RECAPTCHA_SCORE_THRESHOLD=0.5

# URL вашего проекта Supabase для сохранения лидов.
# Если не указан, API будет работать в MOCK-режиме (логи в консоль).
SUPABASE_URL=<your_supabase_project_url>

# Сервисный (service_role) ключ вашего проекта Supabase.
# НЕ используйте публичный (anon) ключ, так как это может нарушить политики RLS.
SUPABASE_KEY=<your_supabase_service_role_key>


# --- ПЕРЕМЕННЫЕ ДЛЯ СБОРКИ И CI/CD ---

# Название вашей организации в Sentry. Используется при сборке для загрузки source maps.
SENTRY_ORG=<your_sentry_organization_slug>

# Название вашего проекта в Sentry.
SENTRY_PROJECT=<your_sentry_project_slug>

# Токен авторизации Sentry. Необходимо создать в настройках Sentry и добавить в секреты CI.
# Имеет права: org:read, project:releases, project:write.
SENTRY_AUTH_TOKEN=<your_sentry_auth_token>